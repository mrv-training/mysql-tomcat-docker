version: '3.3'
services:
  # certbot:
    # image: certbot/certbot
    # container_name: certbot
    # volumes:
      # - /etc/letsencrypt:/etc/letsencrypt
    # command: certonly --standalone --email kbvktv@gmail.com --agree-tos --no-eff-email --force-renewal -d demo-ymt.magrabbit.com -d www.demo-ymt.magrabbit.com
  db:
    image: mysql:8.0
    container_name: mysql_server
    networks:
      - my_network
    command: ['--default-authentication-plugin=mysql_native_password', '--lower_case_table_names=1']
    volumes:
      - db-data:/var/lib/mysql
      - ./mysql-dump:/docker-entrypoint-initdb.d
    env_file:
      - .env
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: ${MYSQL_DATABASE}
      MYSQL_USER: ${MYSQL_USER}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
    ports:
      - 3306:3306
  phpmyadmin:
    depends_on:
      - db
    image: phpmyadmin/phpmyadmin
    container_name: phpmyadmin
    networks:
      - my_network
    ports:
      - '8081:80'
    environment:
      PMA_HOST: db
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
  web:
    build: ./tomcat
    depends_on:
      - db
    image: tomcat:9.0.70-jdk11-corretto-al2
    container_name: webapps
    networks:
      - my_network
    volumes:
      - tomcat:/usr/local/tomcat
      - ./sample-project:/opt/sample-project
      - /etc/letsencrypt:/etc/letsencrypt
    ports:
      - '80:8080'
      - '443:8443' 
  # jenkins:
    # image: jenkins/jenkins:lts
    # privileged: true
    # user: root
    # ports:
      # - 8080:8080
      # - 50000:50000
    # container_name: jenkins
    # volumes:
      # - ./jenkins:/var/jenkins_home
      # - /var/run/docker.sock:/var/run/docker.sock
  # redis:
    # image: 'bitnami/redis:latest'
    # container_name: redis
    # env_file:
      # - .env
    # ports:
      # - '6379:6379'
    # environment:
      # - REDIS_MASTER_PORT_NUMBER=6379
      # - REDIS_PASSWORD=${REDIS_HOST_PASSWORD}
    # volumes:
      # - ./redis:/bitnami/redis/data    
volumes:
  db-data:
  tomcat:
networks:
  my_network:
    driver: bridge